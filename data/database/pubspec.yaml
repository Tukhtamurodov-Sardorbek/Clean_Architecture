name: database
description: A new Flutter package project.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=2.18.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_secure_storage: ^6.0.0
  hive_flutter: ^1.1.0

  core:
    path: ../../domain/core

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.10.0
  build_runner: ^2.3.2
  injectable_generator: ^1.5.5

flutter:
